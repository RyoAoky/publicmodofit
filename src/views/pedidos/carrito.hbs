<style>
    .section-header {
        padding: 140px 0 60px;
        background: var(--negro);
        text-align: center;
    }

    .section-header h1 {
        font-size: 48px;
        color: #fff;
        margin-bottom: 10px;
    }

    .section-cart {
        padding: 40px 0 100px;
        background: var(--negro);
    }

    .cart-row {
        display: flex;
        flex-wrap: wrap;
        gap: 30px;
    }

    .cart-items-box {
        flex: 2;
        min-width: 300px;
    }

    .cart-summary-box {
        flex: 1;
        min-width: 280px;
    }

    .cart-card {
        background: var(--negro-card);
        border: 1px solid var(--borde);
    }

    .cart-card-header {
        padding: 20px 25px;
        border-bottom: 1px solid var(--borde);
    }

    .cart-card-header h5 {
        color: #fff;
        font-size: 14px;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin: 0;
    }

    .cart-table {
        width: 100%;
    }

    .cart-table thead {
        background: var(--gris-oscuro);
    }

    .cart-table th {
        padding: 15px 20px;
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: #777;
        font-weight: 600;
    }

    .cart-table td {
        padding: 20px;
        border-bottom: 1px solid var(--borde);
        color: var(--gris-texto);
    }

    .cart-table tr:last-child td {
        border-bottom: none;
    }

    .item-name {
        font-weight: 500;
        color: #fff;
    }

    .item-type {
        font-size: 11px;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .qty-control {
        display: flex;
        align-items: center;
        gap: 5px;
    }

    .qty-btn {
        width: 32px;
        height: 32px;
        background: var(--gris-oscuro);
        border: 1px solid var(--borde);
        color: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s;
    }

    .qty-btn:hover {
        background: var(--beige);
        border-color: var(--beige);
        color: #000;
    }

    .qty-value {
        width: 40px;
        text-align: center;
        background: var(--gris-oscuro);
        border: 1px solid var(--borde);
        color: #fff;
        padding: 6px;
    }

    .btn-remove {
        background: transparent;
        border: 1px solid #dc3545;
        color: #dc3545;
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s;
    }

    .btn-remove:hover {
        background: #dc3545;
        color: #fff;
    }

    .cart-empty {
        text-align: center;
        padding: 60px 20px;
    }

    .cart-empty i {
        font-size: 64px;
        color: #333;
        margin-bottom: 20px;
    }

    .cart-empty p {
        color: var(--gris-texto);
        margin-bottom: 25px;
    }

    .summary-row {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        color: var(--gris-texto);
    }

    .summary-row.total {
        border-top: 1px solid var(--borde);
        margin-top: 15px;
        padding-top: 20px;
    }

    .summary-row.total span:last-child {
        color: var(--beige);
        font-size: 28px;
        font-family: "Poppins", sans-serif;
        font-weight: 700;
    }

    .btn-checkout {
        display: block;
        width: 100%;
        background: var(--beige);
        border: 1px solid var(--beige);
        color: #000;
        font-family: "Poppins", sans-serif;
        font-weight: 800;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 1px;
        padding: 16px;
        text-align: center;
        text-decoration: none;
        transition: all 0.3s;
        margin-bottom: 12px;
    }

    .btn-checkout:hover {
        background: transparent;
        color: var(--beige);
    }

    .btn-checkout.disabled {
        opacity: 0.5;
        pointer-events: none;
    }

    .btn-continue {
        display: block;
        width: 100%;
        background: transparent;
        border: 1px solid var(--borde);
        color: var(--gris-texto);
        font-family: "Poppins", sans-serif;
        font-weight: 600;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 1px;
        padding: 14px;
        text-align: center;
        text-decoration: none;
        transition: all 0.3s;
    }

    .btn-continue:hover {
        border-color: var(--beige);
        color: var(--beige);
    }

    .iconos-titulo {
        margin: 20px 0;
    }

    .iconos-titulo i {
        color: var(--beige);
        font-size: 4px;
        margin: 0 5px;
    }

    .btn-shop {
        display: inline-block;
        background: var(--beige);
        border: 1px solid var(--beige);
        color: #000;
        font-family: "Poppins", sans-serif;
        font-weight: 700;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 1px;
        padding: 14px 30px;
        text-decoration: none;
        transition: all 0.3s;
    }

    .btn-shop:hover {
        background: transparent;
        color: var(--beige);
    }
</style>

<!-- Header -->
<section class="section-header">
    <div class="container">
        <h1><i class="bi bi-cart3 me-3"></i>MI CARRITO</h1>
        <div class="iconos-titulo">
            <i class="bi bi-circle-fill"></i>
            <i class="bi bi-circle-fill"></i>
            <i class="bi bi-circle-fill"></i>
        </div>
    </div>
</section>

<!-- Cart Section -->
<section class="section-cart">
    <div class="container">
        <div class="cart-row">
            <!-- Cart Items -->
            <div class="cart-items-box">
                <div class="cart-card">
                    <div id="carrito-items">
                        <!-- Items loaded via JS -->
                    </div>
                    <div id="carrito-vacio" class="cart-empty" style="display: none;">
                        <i class="bi bi-cart-x"></i>
                        <p>Tu carrito está vacío</p>
                        <a href="/pedidos" class="btn-shop">
                            <i class="bi bi-shop me-2"></i>Ir a la tienda
                        </a>
                    </div>
                </div>
            </div>

            <!-- Summary -->
            <div class="cart-summary-box">
                <div class="cart-card" style="position: sticky; top: 100px;">
                    <div class="cart-card-header">
                        <h5>Resumen del Pedido</h5>
                    </div>
                    <div style="padding: 25px;">
                        <div class="summary-row">
                            <span>Subtotal</span>
                            <span id="subtotal">S/ 0.00</span>
                        </div>
                        <div class="summary-row">
                            <span>IGV (18%)</span>
                            <span id="igv">S/ 0.00</span>
                        </div>
                        <div class="summary-row total">
                            <span style="color: #fff; font-weight: 600;">Total</span>
                            <span id="total">S/ 0.00</span>
                        </div>
                    </div>
                    <div style="padding: 0 25px 25px;">
                        <a href="/pedidos/checkout" class="btn-checkout" id="btn-checkout">
                            <i class="bi bi-credit-card me-2"></i>Proceder al Pago
                        </a>
                        <a href="/pedidos" class="btn-continue">
                            <i class="bi bi-arrow-left me-2"></i>Seguir Comprando
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
let carrito = JSON.parse(localStorage.getItem('modofit_carrito')) || [];

function renderCarrito() {
    const container = document.getElementById('carrito-items');
    const vacioMsg = document.getElementById('carrito-vacio');
    const btnCheckout = document.getElementById('btn-checkout');
    
    if (carrito.length === 0) {
        container.style.display = 'none';
        vacioMsg.style.display = 'block';
        btnCheckout.classList.add('disabled');
        return;
    }
    
    container.style.display = 'block';
    vacioMsg.style.display = 'none';
    btnCheckout.classList.remove('disabled');
    
    let html = `
        <table class="cart-table">
            <thead>
                <tr>
                    <th>Producto</th>
                    <th style="text-align: center;">Cantidad</th>
                    <th style="text-align: right;">Precio</th>
                    <th style="text-align: right;">Subtotal</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
    `;
    
    carrito.forEach((item, index) => {
        html += `
            <tr>
                <td>
                    <div class="item-name">${item.nombre}</div>
                    <div class="item-type">${item.tipo === 'membresia' || item.tipo === 'plan' ? 'Membresía' : 'Producto'}</div>
                </td>
                <td>
                    <div class="qty-control" style="justify-content: center;">
                        <button class="qty-btn" onclick="cambiarCantidad(${index}, -1)">-</button>
                        <span class="qty-value">${item.cantidad}</span>
                        <button class="qty-btn" onclick="cambiarCantidad(${index}, 1)">+</button>
                    </div>
                </td>
                <td style="text-align: right;">S/ ${item.precio.toFixed(2)}</td>
                <td style="text-align: right; color: #fff; font-weight: 600;">S/ ${(item.precio * item.cantidad).toFixed(2)}</td>
                <td style="text-align: right;">
                    <button class="btn-remove" onclick="eliminarItem(${index})">
                        <i class="bi bi-trash"></i>
                    </button>
                </td>
            </tr>
        `;
    });
    
    html += '</tbody></table>';
    container.innerHTML = html;
    
    actualizarTotales();
}

function cambiarCantidad(index, delta) {
    carrito[index].cantidad += delta;
    if (carrito[index].cantidad <= 0) {
        carrito.splice(index, 1);
    }
    guardarYRender();
}

function eliminarItem(index) {
    carrito.splice(index, 1);
    guardarYRender();
}

function guardarYRender() {
    localStorage.setItem('modofit_carrito', JSON.stringify(carrito));
    renderCarrito();
}

function actualizarTotales() {
    const subtotal = carrito.reduce((sum, item) => sum + (item.precio * item.cantidad), 0);
    const igv = subtotal * 0.18;
    const total = subtotal;
    
    document.getElementById('subtotal').textContent = 'S/ ' + (subtotal - igv).toFixed(2);
    document.getElementById('igv').textContent = 'S/ ' + igv.toFixed(2);
    document.getElementById('total').textContent = 'S/ ' + total.toFixed(2);
}

document.addEventListener('DOMContentLoaded', renderCarrito);
</script>
